{
  "name": "pi-browser-control",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that registers the browser_control tool — talks to a local Firefox add-on via native messaging + a shared daemon.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "typebox": "^1.3.2"
  },
  "devDependencies": {
    "web-ext": "^10.4.0"
  }
}
