{
  "name": "pi-plan-mode",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that adds a read-only 'plan mode' — restricts tools to read/bash/grep/find/ls/questionnaire, allowlists bash to read-only commands, extracts numbered plan steps from a Plan: section, tracks [DONE:n] completion markers, and shows a progress widget during execution.",
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@mariozechner/pi-agent-core": "*",
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*"
  }
}
