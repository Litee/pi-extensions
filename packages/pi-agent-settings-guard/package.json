{
  "name": "pi-agent-settings-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that prevents the LLM from editing settings files in the wrong location and instructs it to use the correct files instead.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.1"
  }
}
