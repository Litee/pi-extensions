{
  "name": "pi-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that registers a /tools command for reviewing every tool available in a pi session (name, source, description, parameter schema, active state, and a compact token estimate).",
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
