{
  "name": "pi-update-cmux-status",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that mirrors pi lifecycle events into cmux (sidebar status pill, log lines, notifications, progress) and auto-renames the cmux tab + workspace based on an LLM summary of the first user prompt.",
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*"
  }
}
