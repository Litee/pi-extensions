{
  "name": "pi-cmux-cli-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that blocks bash tool calls referencing focused.workspace_ref and directs the LLM to use caller.workspace_ref from cmux identify --json instead.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.1"
  }
}
