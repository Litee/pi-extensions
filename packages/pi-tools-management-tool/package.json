{
  "name": "pi-tools-management-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension that registers a manage_tools tool so the LLM can list, activate, deactivate, and reset its own tool set at runtime.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@earendil-works/pi-tui": "^0.84.1",
    "typebox": "^1.3.2"
  }
}
